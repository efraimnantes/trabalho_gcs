class JInteger:
    def __init__(self, value: int):
        self.value = value
def floatValue(self) -> float:
        """Equivalente ao Integer.floatValue() do Java.
        Retorna o valor interno convertido para float primitivo."""
        return float(self.value)        